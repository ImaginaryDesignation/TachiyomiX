CREATE TABLE IF NOT EXISTS manga_backup(
                               id INTEGER NOT NULL PRIMARY KEY,
                               manga_id INTEGER NOT NULL,
                               thumbnail_url TEXT,
                               backup_time INTEGER AS Long NOT NULL
                           );